type TProps = {
book: TBook;
onClose: () => void;
}; yazmisam
<br>
onClose: () => void; niye yaziriq onClose: void yazmiriq?
<br>

bunlari qarisdiriram biraz
onClick={() => onDelete(book.name)}
onClick={onClose}
